import GET_LIST from '../actions/types'

const initialState = {
  dogBreeds: null
}

export default function (state = initialState, action) {
  switch (action.type) {
    
    default:
      return state
  }
}